#ifndef INCLUDED_SORT_
#define INCLUDED_SORT_

#include <iostream>
#include <string>

class Sort
{
    public:
        // Sort(int (*function)(string **string1, string **string2));
        Sort();
        void sort(std::string *array, size_t elements);

    private:
};
        
#endif
