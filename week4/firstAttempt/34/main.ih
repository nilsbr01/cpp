#include <iostream>
#include <string>

#include "person/person.h"

namespace Icmbuild
{
    extern char version[];
    extern char years[];
    extern char author[];
};

void usage(std::string const &progname);
void extract(Person person[], size_t size);
void insert(Person person[], size_t size);

using namespace std;
